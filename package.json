{
  "name": "kanban-desafio-monorepo",
  "private": true,
  "workspaces": [
    "app/backend",
    "app/frontend"
  ],
  "scripts": {
    "test": "npm test -ws --if-present"
  },
  "devDependencies": {
  }
}
